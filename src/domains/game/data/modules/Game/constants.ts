import { standardPoolSize } from 'domains/game/data/modules/Sign';

export const initialSequences = standardPoolSize;
export const initialAttempts = 3;
export const increaseScoreFactor = 100;
// export const increaseScoreFactor = initialSequences === 4 ? 100 : 200;
